[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "tw-housing-analysis"
version = "0.1.0"
description = "Taiwan housing data project"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]